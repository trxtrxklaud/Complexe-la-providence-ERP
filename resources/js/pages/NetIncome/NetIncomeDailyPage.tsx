import { NetIncomeReportPage } from '../Treasury/NetIncomeReportPage';

/**
 * الدخل الصافي اليومي.
 *
 * الشاشة اليومية مبنية من قبل ومتصلة بـ /reports/net-income ومجرّبة على بيانات حقيقية.
 * إعادة كتابتها داخل الموديول الجديد كانت ستنتج شاشتين تقرأان نفس الرقم وتتباعدان
 * مع أول تعديل يُنسى في إحداهما، فالموديول يعيد استعمالها لا ينسخها.
 *
 * نقل الملف نفسه إلى هذا المجلد خطوة تنظيمية مؤجّلة إلى ما بعد دمج الفرع،
 * لأن نقل ملفات أثناء وجود PR مفتوح يصعّب المراجعة دون فائدة وظيفية.
 */
export function NetIncomeDailyPage() {
  return <NetIncomeReportPage />;
}
