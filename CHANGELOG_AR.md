# طبقة التلميع البصري والتفاعلي (Polish Layer)

**تاريخ التقرير:** 24 أغسطس 2026

## 1. الملخص التنفيذي

طبقة **تجميلية وتفاعلية إضافية بحتة** فوق البنية القائمة — لا إعادة بناء. الهدف: رفع الإحساس البصري للمنصة إلى مستوى SaaS تعليمي عالمي مع صون هوية المدرسة (أخضر زيتوني `#2E3B2A` + نحاسي `#C2A24E` + عاجي `#F7F5EF`).

> **قيد صارم محترَم بالكامل:** لا تغيير في أيّ نصّ/تسمية/محتوى ظاهر، ولا في منطق الأعمال، ولا الـAPI، ولا الـroutes، ولا الـprops، ولا إدارة الحالة. صفر مساس بالـbackend أو بالتدفّقات المالية الحسّاسة. كلّ نداءات `alert()`/`confirm()` في المسارات المالية بقيت سليمة.

## 2. نطاق التغيير (بصري/تفاعلي فقط)

| المجال | ما أُضيف |
|---|---|
| أساس عامّ (CSS) | خطّ Inter للّاتيني/الأرقام + IBM Plex Sans Arabic للعربية؛ حلقة تركيز نحاسية (`:focus-visible`)؛ أشرطة تمرير رفيعة بلون الهوية؛ `tabular-nums` لمحاذاة أرقام الجداول؛ تظليل نصّ بلون الهوية؛ shimmer للهياكل؛ أداة `.card-interactive` (رفع لطيف) — كلّها opt-in |
| الشريط الجانبي | قابل للطيّ إلى شريط أيقونات (256px ⇄ 76px)، محفوظ في `localStorage`، مع تلميحات عائمة عبر portal عند الطيّ. **الروابط/الصلاحيات/الخروج دون أيّ مساس** |
| القشرة | انتقال دخول لطيف بين الموديولات (`motion` مُفتاحه أوّل مقطع من المسار حتى لا تُعاد تهيئة الموديولات الفرعية) |
| الهياكل | ترقية `animate-pulse` إلى shimmer بلون الهوية (نفس الـexports) |
| نظام Toast (جديد) | `ToastProvider`/`useToast()`/`<Toaster>` — success/error/info، RTL، إخفاء تلقائي |
| EmptyState (جديد) | إليستريشن SVG inline بلون الهوية (آمن دون اتصال) |
| اللوحة الرئيسية | كروت KPI: رفع عند المرور + ظلّ موحّد + دخول متدرّج؛ مودال الديون السابقة: خلفية ضبابية + دخول متحرّك — **نفس التسميات والقيم والبيانات، بلا اتّجاهات/رسوم مختلقة** |

## 3. الملفات المعدَّلة

**البنية التحتية والقشرة:** `resources/css/app.css` · `resources/views/app.blade.php` (رابط خطّ Inter) · `resources/js/App.tsx` (انتقالات + `MotionConfig reducedMotion="user"` + `ToastProvider`) · `resources/js/components/Sidebar.tsx` · `resources/js/components/DataSkeleton.tsx`

**مكوّنات جديدة:** `resources/js/components/ui/Toast.tsx` · `resources/js/components/EmptyState.tsx`

**تلميع صفحات + ربط محدود (نفس النصّ حرفيًا):** `resources/js/pages/Dashboard/Dashboard.tsx` · `pages/Users/UsersList.tsx` و`pages/FeeTypes/FeeTypesPage.tsx` (خطأ الحذف: `alert()` → `toast.error()` بنفس النصّ) · `pages/Families/FamiliesListPage.tsx` (حالة الفراغ غير الجدولية → `EmptyState` بنفس النصّ)

## 4. إصلاح حرج في تقسيم الحزم (vite.config.ts)

**العرض:** شاشة بيضاء كاملة عند الإقلاع بعد إضافة `motion` — `TypeError: Cannot read properties of undefined (reading 'createContext')`.

**السبب الجذري:** بين `react-vendor` (عبر `react-router-dom → history`) وحزمة `vendor` **اعتماد دائري**. لم يكن يضرّ سابقًا لأنّ لا شيء في `vendor` يلمس React وقت التهيئة. لكن `motion` ينشئ سياقات React (`createContext`) عند تحميل الوحدة، فوقع على React قبل أن تُهيَّأ حزمته → انهيار.

**الحلّ:** نقل الحزم الأربع `motion` · `motion-dom` · `motion-utils` · `framer-motion` إلى مجموعة `react-vendor` لتُهيَّأ مع React في نفس الحزمة. بعده انكمش `vendor` من ‎133kB إلى ‎3.85kB، ونما `react-vendor` تبعًا لذلك.

> **قاعدة للمستقبل:** أيّ مكتبة تستدعي `createContext`/تلمس React وقت التحميل يجب أن تُوضَع في `react-vendor` لا في `vendor`، وإلّا عادت الشاشة البيضاء. لم يُرفَع `chunkSizeWarningLimit` ولم يُحرَّر ناتج `public/build` يدويًا.

## 5. التحقّق

- `tsc --noEmit`: نظيف. `vite build`: نجح بلا تحذيرات حدود chunks.
- شاشة الدخول تُصيَّر سليمة على الأساس الجديد (Reem Kufi للعنوان + Inter للنصّ)؛ قواعد الأساس الستّ حاضرة في الـCSS المبني.
- القشرة خلف المصادقة (الطيّ/الانتقالات/الكروت/الـtoast/الفراغ): **أكّد المستخدم أنّ المنصة تعمل جيدًا** بعد الإصلاح.
- كلّ الحركات تحترم `prefers-reduced-motion`.

---

# التقرير التفصيلي للتغييرات المنفذة في مشروع Complexe la Providence ERP

**تاريخ التقرير:** 14 أغسطس 2026

## 1. الملخص التنفيذي

تم تنفيذ مجموعة مترابطة من التغييرات لتحويل رسوم النوادي من سجلات منفصلة عن رسوم التلميذ إلى جزء واضح من رصيد التلميذ والتحصيل والجرد. أصبح النظام ينشئ دين النادي تلقائيًا عند استخلاص المعلوم الشهري، ويتركه كمتخلد إذا لم يتم دفعه، مع إمكانية قبضه لاحقًا داخل نفس شاشة الاستخلاص أو عبر صفحة النوادي. كما تم إنشاء Dashboard إحصائي مستقل يبين إجمالي المتخلدات حسب الأقسام والتلاميذ والنوادي، ويقدم جدولًا تفصيليًا قابلًا للطباعة.

> النتيجة العملية الأساسية: عدم دفع معلوم النادي لم يعد يؤدي إلى اختفاء الدين أو بقائه في تقرير منفصل فقط؛ بل أصبح جزءًا من الرصيد المالي الموحد للتلميذ، مع تحديث تلقائي عند الدفع أو الإلغاء أو تغيير السعر.

## 2. نطاق التغيير الوظيفي

قبل هذه التغييرات كانت رسوم النادي تُدار أساسًا من خلال جدول `club_monthly_fees`، بينما كان حساب رصيد التلميذ يعتمد على `student_fees` وتخصيصات الدفعات. هذا الفصل كان يجعل معلوم النادي غير المدفوع لا يظهر دائمًا ضمن متخلدات التلميذ العامة، كما لم تكن شاشة الاستخلاص تعرض رسوم النادي كخيارات قابلة للقبض داخل نفس الوصل.

بعد التغيير أصبح لكل رسم نادي شهري سجل مقابل في `student_fees`. هذا السجل يحمل رابطًا مباشرًا إلى رسم النادي، ويُستخدم لحساب المتخلدات والتخصيصات والتقارير، مع الاحتفاظ بإمكانية وجود خلاص مباشر من صفحة النوادي.

| المجال | السلوك الجديد |
|---|---|
| إنشاء رسوم النادي | إنشاء سجل `StudentFee` مرتبط تلقائيًا بكل `ClubMonthlyFee` جديد |
| عدم دفع النادي | إبقاء الرصيد كمتخلد في حساب التلميذ |
| الدفع من الاستخلاص | اختيار رسوم النادي وقبضها داخل نفس الوصل مع المعلوم الشهري |
| الدفع من صفحة النادي | تحديث رصيد التلميذ تلقائيًا دون احتساب مزدوج |
| إلغاء الدفع | إعادة المتخلد وتحديث حالة النادي والتلميذ |
| تغيير سعر النادي | تحديث الرسوم الحالية غير المدفوعة وسجل دين التلميذ المرتبط |
| الجرد | طرح المدفوعات المباشرة ومدفوعات الاستخلاص من المتخلد |
| Dashboard | تجميع إجمالي المتخلدات حسب القسم والتلميذ والنادي مع طباعة A4 |

## 3. تغييرات قاعدة البيانات

أضيف ملف migration جديد:

```text
database/migrations/2026_08_14_000003_link_club_monthly_fees_to_student_fees.php
```

أضاف هذا الترحيل إلى جدول `student_fees` الحقلين التاليين:

| الحقل | الوظيفة |
|---|---|
| `club_monthly_fee_id` | رابط اختياري وفريد إلى رسم النادي الشهري المقابل |
| `direct_paid_amount` | قيمة ما تم دفعه مباشرة من صفحة النوادي، منفصلة عن تخصيصات الوصل |

كما يقوم الترحيل بإنشاء سجلات `StudentFee` للرسوم الموجودة سابقًا في `club_monthly_fees`، حتى لا تضيع المتخلدات التاريخية عند تشغيل النظام على بيانات قديمة. ويحتوي الترحيل على `down()` لإزالة الحقول والفهارس التي أضافها.

ملاحظة مهمة: المرجع الأساسي لدفعات الاستخلاص هو `student_fee_id` الموجود أصلًا في `payment_allocations`. أضيفت علاقة اختيارية في النموذج لدعم الربط المباشر، لكن عدم دفع النادي لا يحتاج إلى إنشاء تخصيص مستقل خارج تخصيص رسم التلميذ المرتبط.

## 4. تغييرات النماذج والعلاقات

### 4.1 نموذج `StudentFee`

الملف:

```text
app/Models/StudentFee.php
```

تمت إضافة `club_monthly_fee_id` و`direct_paid_amount` إلى الحقول القابلة للتعبئة، مع تحويل `direct_paid_amount` إلى قيمة عشرية من خانتين.

أضيفت العلاقة التالية:

```php
public function clubMonthlyFee(): BelongsTo
```

كما أضيفت الدالة:

```php
public function directPaidAmount(): float
```

وأُعيد تعريف حساب `outstanding()` ليصبح:

```text
المتخلد = المبلغ المستحق - تخصيصات الدفعات - الدفع المباشر - التنازلات السارية
```

وبذلك لا يظهر المبلغ المدفوع مباشرة من صفحة النوادي كدين متبقٍ على التلميذ.

### 4.2 نموذج `ClubMonthlyFee`

الملف:

```text
app/Models/ClubMonthlyFee.php
```

أضيفت علاقة `studentFee()` من نوع `HasOne` للوصول إلى سجل دين التلميذ المرتبط برسم النادي الشهري.

### 4.3 نموذج `PaymentAllocation`

الملف:

```text
app/Models/PaymentAllocation.php
```

أضيفت قابلية التعامل مع `club_monthly_fee_id` وعلاقة `clubMonthlyFee()`. أما التدفق الفعلي للاستخلاص المدمج فيستخدم سجل `StudentFee` المرتبط، وهو ما يضمن أن الرصيد العام للتلميذ والدفتر النقدي يقرآن من نفس مصدر التخصيص.

## 5. تغييرات خدمة النوادي

الملف الرئيسي:

```text
app/Services/ClubService.php
```

### 5.1 إنشاء دين التلميذ تلقائيًا

أضيفت الدالة الداخلية `ensureStudentFeeForClubMonthlyFee()`. وظيفتها إنشاء سجل `StudentFee` مرتبط برسم النادي إذا لم يكن موجودًا، مع حفظ:

| البيانات | القيمة |
|---|---|
| التسجيل | `enrollment_id` الخاص بالتلميذ |
| الوصف | معلوم النادي واسم الشهر |
| المبلغ المستحق | قيمة `ClubMonthlyFee.amount_due` |
| الدفع المباشر | قيمة الدفع المباشر الحالي |
| الحالة | pending أو partial حسب وضع الرسم |

### 5.2 إنشاء رسوم النادي أثناء الاستخلاص

أضيفت الدالة العامة `ensureFeesForEnrollment()`. تبحث عن اشتراكات النوادي النشطة للتلميذ والسنة الدراسية، ثم تنشئ سجلات الأشهر المطلوبة فقط، وتضمن وجود سجل دين تلميذ مقابل كل رسم نادي.

يتم استدعاؤها من معاينة الاستخلاص ومن عملية تثبيت الدفع. وبذلك تصبح رسوم النادي متاحة قبل اختيارها للدفع، وتبقى غير المختارة كمتخلد.

### 5.3 تحيين الأسعار

تم تعديل `syncUnpaidCurrentMonthFees()` بحيث لا يغير فقط `ClubMonthlyFee.amount_due`، بل يحدث أيضًا `StudentFee.amount_due` المرتبط به.

التحديث يطبق فقط على السجلات الحالية غير المدفوعة. السجلات المدفوعة أو الجزئية لا يتم تغييرها حفاظًا على الأثر المالي والتدقيق.

### 5.4 الدفع المباشر والإلغاء

تم تعديل `recordPayment()` ليحسب الجزء المدفوع مباشرة بعد طرح التخصيصات الموجودة، ثم يحفظه في `direct_paid_amount`. هذه الخطوة تمنع اعتبار نفس المبلغ مدفوعًا مرتين.

وعند إلغاء الدفع، يتم:

1. إلغاء الحركة المالية.
2. إعادة `direct_paid_amount` إلى الصفر.
3. الإبقاء على أي تخصيصات صحيحة من وصل سابق.
4. إعادة حساب حالة رسم النادي وحالة دين التلميذ.

## 6. تغييرات خدمة التحصيل

الملف:

```text
app/Services/CollectionService.php
```

### 6.1 معاينة رسوم النوادي

أصبحت دالة `preview()` تعيد قائمة `club_items` تتضمن رسوم النوادي غير المسددة أو المسددة جزئيًا للتلميذ. تحتوي كل خانة على:

| الحقل | المعنى |
|---|---|
| `club_monthly_fee_id` | معرف رسم النادي |
| `student_fee_id` | معرف دين التلميذ المرتبط |
| `month` | الشهر |
| `club_name` | اسم النادي |
| `amount_due` | المبلغ المستحق |
| `amount_paid` | المبلغ المدفوع |
| `remaining_amount` | المتخلد المتبقي |
| `status` | حالة الرسم |

ولا تقتصر القائمة على الشهر المحدد فقط؛ بل تعرض أيضًا المتخلدات السابقة للتلميذ، حتى يمكن قبضها مع وصل الشهر الحالي.

### 6.2 تثبيت دفع مختلط

تم توسيع `collect()` لقبول الحقل التالي:

```json
{
  "club_items": [
    {
      "club_monthly_fee_id": 123,
      "amount": 50
    }
  ]
}
```

عند التثبيت يتحقق النظام من أن الرسم يخص نفس تسجيل التلميذ، وأن المبلغ لا يتجاوز المتخلد. ثم يقوم بـ:

1. تحديث `ClubMonthlyFee.amount_paid` و`status`.
2. إنشاء تخصيص دفع على `StudentFee` المرتبط.
3. إعادة حساب حالة دين التلميذ.
4. إضافة بند النادي إلى الوصل.
5. تسجيل الحركة في الدفتر النقدي كبند معاليم نوادٍ.

إذا لم يتم إرسال الرسم في `club_items` فلا يتم قبضه، وإنما يبقى متخلدًا ظاهرًا في حساب التلميذ والتقارير.

### 6.3 التحقق من الطلب

الملف:

```text
app/Http/Requests/CollectPaymentRequest.php
```

أضيف تحقق الحقول:

```text
club_items
club_items.*.club_monthly_fee_id
club_items.*.amount
```

ويشمل التحقق منع تكرار نفس رسم النادي، والتحقق من وجوده، ورفض المبالغ الصفرية أو الأكبر من المتبقي.

## 7. تغييرات حساب الرصيد والدفتر والجرد

### 7.1 خدمة الدفع

الملف:

```text
app/Services/PaymentService.php
```

تم تعديل حساب المبلغ المتبقي وحالة رسم التلميذ ليأخذ في الاعتبار `directPaidAmount()`.

كما أضيفت مزامنة عكسية: عند إعادة حساب حالة `StudentFee` المرتبط بنادٍ، يتم تحديث `ClubMonthlyFee.amount_paid` و`ClubMonthlyFee.status` بناءً على:

```text
إجمالي المدفوع = التخصيصات + المدفوع المباشر
```

### 7.2 الدفتر النقدي

الملف:

```text
app/Services/LedgerService.php
```

أصبح أي `StudentFee` يحمل `club_monthly_fee_id` يصنف تلقائيًا ضمن:

```text
CashTransaction::CATEGORY_CLUB_FEE
```

وبذلك لا تختلط مداخيل النوادي مع القسط الشهري أو التسجيل أو النقل.

### 7.3 API رسوم التلميذ

الملف:

```text
app/Http/Controllers/PaymentController.php
```

تم تعديل حقل `remaining` في استجابة رسوم التلميذ ليطرح المدفوع المباشر، كما أضيف الحقل `direct_paid` لتمكين الواجهة من عرض تفاصيل الحساب بوضوح.

### 7.4 جرد القسم

الملف:

```text
app/Http/Controllers/ClassroomRosterController.php
```

تم تعديل حساب المتخلد ليطرح `direct_paid_amount` من إجمالي المبالغ المستحقة، فلا يظهر الدفع المباشر للنادي كرصيد متخلد في جرد القسم.

## 8. واجهة الاستخلاص

الملف:

```text
resources/js/pages/Payments/CollectionPage.tsx
```

تمت إضافة حالة اختيار رسوم النوادي ومبالغها، مع عرض قسم مستقل بعنوان:

```text
متخلدات النوادي للشهور المختارة
```

تتيح الواجهة للقابض تحديد كل معلوم نادي أو إدخال مبلغ جزئي. كما تم تحديث:

| العنصر | التغيير |
|---|---|
| إجمالي الوصل | يشمل القسط ورسوم النوادي والمتخلدات السابقة |
| الحفظ | يرسل `club_items` إلى API |
| المعاينة | تعرض المتخلدات السابقة والحالية |
| الإعفاء الكامل | لا يمنع قبض النادي أو المتخلدات السابقة إذا كان هناك مبلغ فعلي للدفع |
| الوصل | يضم رسوم النوادي كبنود منفصلة |

## 9. Dashboard المتخلدات

تم إنشاء صفحة جديدة:

```text
resources/js/pages/Reports/ClubArrearsDashboardPage.tsx
```

وتم تسجيل المسار:

```text
/reports/club-arrears
```

كما أضيف رابط في الشريط الجانبي باسم:

```text
Dashboard متخلدات النوادي
```

### 9.1 API الجديد

تمت إضافة المسار:

```http
GET /api/reports/club-arrears
```

ويقبل الفلاتر التالية:

| الفلتر | الوظيفة |
|---|---|
| `academic_year_id` | السنة الدراسية |
| `club_id` | نادٍ محدد |
| `level_id` | مستوى محدد |
| `section_id` | قسم محدد |
| `search` | البحث باسم التلميذ أو رقمه |

ويحمي المسار نفس صلاحيات تقارير النوادي الحالية: `manage_payments` أو `view_reports` عبر حماية الواجهة، و`manage_payments` على API.

### 9.2 المؤشرات المعروضة

يعيد Dashboard المؤشرات التالية:

| المؤشر | الوصف |
|---|---|
| `total_remaining` | إجمالي المتخلدات غير المسددة |
| `students_count` | عدد التلاميذ الذين لديهم متخلدات |
| `sections_count` | عدد الأقسام التي تحتوي متخلدات |
| `clubs_count` | عدد النوادي التي تحتوي متخلدات |
| `fees_count` | عدد رسوم النادي غير المسددة أو الجزئية |
| `total_due` | مجموع المبالغ المستحقة في السجلات المعروضة |
| `total_paid` | مجموع ما تم دفعه في تلك السجلات |

### 9.3 التجميع حسب القسم والتلميذ

ينتج API مجموعتين رئيسيتين:

1. `sections`: تحتوي كل قسم، عدد التلاميذ، عدد النوادي، عدد الرسوم، إجمالي المتخلدات، وقائمة التلاميذ.
2. `students`: تحتوي اسم التلميذ ورقمه وقسمه وعدد النوادي وعدد الأشهر وإجمالي المتخلدات وتفصيل كل رسم نادي.

### 9.4 الطباعة

أضيف تصميم طباعة خاص بـ A4 أفقي، ويخفي الفلاتر والأزرار أثناء الطباعة. يتضمن التقرير عنوانًا، السنة الدراسية، تاريخ الطباعة، البطاقات الإحصائية، مقارنة الأقسام، ثم الجدول التفصيلي الكامل.

## 10. مسارات وملفات الواجهة الجديدة

| الملف | التغيير |
|---|---|
| `resources/js/App.tsx` | تسجيل lazy route لصفحة Dashboard ومسار `/reports/club-arrears` |
| `resources/js/components/Sidebar.tsx` | إضافة رابط Dashboard |
| `resources/js/api/clubs.ts` | إضافة أنواع `ClubArrearsDetail`, `ClubArrearsStudent`, `ClubArrearsSection`, `ClubArrearsDashboardData` ودالة `fetchClubArrearsDashboard()` |
| `resources/js/pages/Reports/ClubArrearsDashboardPage.tsx` | صفحة Dashboard كاملة بالفلاتر والمؤشرات والجدول والطباعة |
| `resources/js/api/payments.ts` | توسيع أنواع المعاينة والدفع لدعم `club_items` |
| `resources/js/pages/Payments/CollectionPage.tsx` | إضافة اختيار وقبض رسوم النادي داخل الوصل |

## 11. الاختبارات والتحقق

تمت إضافة اختبار في:

```text
tests/Feature/ClubFeesTest.php
```

ويثبت أن Dashboard:

1. يجمع القسم الصحيح.
2. يحسب عدد التلاميذ بشكل صحيح.
3. يحسب إجمالي المتخلدات.
4. يعيد التلميذ الصحيح.
5. يعيد تفاصيل النادي والشهر والمبلغ.

كما أضيف اختبار في:

```text
tests/Feature/CollectionServiceTest.php
```

ويثبت السيناريو التالي:

1. دفع المعلوم الشهري.
2. إنشاء متخلد النادي تلقائيًا.
3. بقاء المتخلد بقيمة صحيحة إذا لم يدفع.
4. قبض متخلد النادي لاحقًا داخل وصل آخر.
5. انتقال حالة رسم النادي إلى `paid`.
6. وصول متخلد دين التلميذ إلى صفر.

### نتائج التشغيل الأخيرة

| الفحص | النتيجة |
|---|---|
| اختبارات Dashboard والنوادي | 23 اختبارًا ناجحًا، 75 assertion |
| مجموعة الاختبارات الكاملة | 190 اختبارًا ناجحًا، 717 assertion |
| TypeScript lint | ناجح |
| بناء الواجهة الإنتاجية | ناجح |
| `git diff --check` | ناجح |

## 12. سيناريوهات الاستخدام بعد التغيير

### السيناريو الأول: دفع القسط دون النادي

يختار القابض التلميذ والشهر ويدفع القسط فقط. ينشئ النظام في الخلفية رسم النادي وسجل دين التلميذ، لكن لا يضيفه إلى الوصل. يظهر المبلغ في Dashboard وفي متخلدات التلميذ كرصيد غير مسدد.

### السيناريو الثاني: دفع القسط والنادي في نفس الوصل

يختار القابض رسم النادي من قسم متخلدات النوادي، ويمكنه دفعه كاملًا أو جزئيًا. يتم إنشاء تخصيص واحد للرسم المرتبط، ويظهر النادي كبند مستقل في الوصل ويصنف في الدفتر تحت معاليم النوادي.

### السيناريو الثالث: دفع متخلد نادي قديم

عند استخلاص شهر جديد، تعرض شاشة الاستخلاص أيضًا المتخلدات السابقة للتلميذ. يمكن قبض معلوم النادي القديم مع الوصل الحالي، حتى لو لم يكن شهر النادي ضمن الأشهر المحددة في الوصل المدرسي.

### السيناريو الرابع: تغيير سعر النادي

عند تغيير السعر من إدارة النوادي، تتحين السجلات الحالية غير المدفوعة وسجلات دين التلاميذ المرتبطة بها. لا تتغير السجلات المدفوعة أو الجزئية.

### السيناريو الخامس: إلغاء الدفع

عند إلغاء الدفع المباشر أو دفع الوصل، يعيد النظام حساب التخصيصات والمدفوع المباشر والحالة والمتخلد دون تكرار أو فقدان للمعلومات المالية.

## 13. الملفات المتغيرة والملفات الجديدة

### ملفات معدلة

```text
app/Http/Controllers/ClassroomRosterController.php
app/Http/Controllers/ClubReportController.php
app/Http/Controllers/PaymentController.php
app/Http/Requests/CollectPaymentRequest.php
app/Models/ClubMonthlyFee.php
app/Models/PaymentAllocation.php
app/Models/StudentFee.php
app/Services/ClubService.php
app/Services/CollectionService.php
app/Services/LedgerService.php
app/Services/PaymentService.php
resources/js/App.tsx
resources/js/api/clubs.ts
resources/js/api/payments.ts
resources/js/components/Sidebar.tsx
resources/js/pages/Payments/CollectionPage.tsx
 tests/Feature/ClubFeesTest.php
 tests/Feature/CollectionServiceTest.php
routes/api.php
```

### ملفات جديدة

```text
database/migrations/2026_08_14_000003_link_club_monthly_fees_to_student_fees.php
resources/js/pages/Reports/ClubArrearsDashboardPage.tsx
```

## 14. خطوات التشغيل المطلوبة على بيئة الإنتاج

بعد رفع التغييرات إلى الخادم، يجب تشغيل migration:

```bash
php artisan migrate
```

ثم إعادة بناء الواجهة:

```bash
npm install
npm run build
```

وبعد ذلك يجب التأكد من أن المستخدمين المخولين يملكون صلاحية `manage_payments` أو `view_reports` للوصول إلى Dashboard من الواجهة، وأن صلاحية API `manage_payments` مفعلة للحسابات التي ستستعمل التقرير.

## 15. ملاحظات وحدود معروفة

Dashboard يعرض الرسوم التي لها سجل `ClubMonthlyFee` فعلي. لذلك فإن رسوم الأشهر التي لم تُولّد ولم يمر التلميذ بعملية إنشاء رسومها لن تظهر حتى يتم توليدها أو استخلاص المعلوم الشهري الذي ينشئها تلقائيًا.

تجميع Dashboard يحسب المتخلد مباشرة من `amount_due - amount_paid` لرسوم النادي. التخفيضات الخاصة بالنوادي موجودة في تقرير النوادي الشهري الحالي، أما Dashboard العام فيتعامل مع الرصيد المسجل في الرسم الشهري نفسه، ولذلك يجب توحيد سياسة الخصومات لاحقًا إذا كان المطلوب أن تظهر التخفيضات التفصيلية داخل Dashboard العام أيضًا.

لم يتم إجراء commit أو push إلى GitHub؛ التغييرات موجودة محليًا في المستودع:

```text
/home/ubuntu/Complexe-la-providence-ERP
```

## 16. الخلاصة

أصبحت رسوم النوادي الآن مرتبطة بدورة مالية متكاملة: إنشاء الدين، ظهوره في رصيد التلميذ، إمكانية قبضه مع المعلوم الشهري، تسجيله في الدفتر، تحيينه عند تغيير السعر، إرجاعه عند الإلغاء، ثم عرضه إحصائيًا حسب القسم والتلميذ في تقرير قابل للطباعة. وقد تم التحقق من سلامة التغيير عبر الاختبارات الكاملة وبناء الواجهة وفحص TypeScript.
