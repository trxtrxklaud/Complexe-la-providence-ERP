<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Super roles
    |--------------------------------------------------------------------------
    |
    | الأدوار التي تتجاوز فحص الصلاحيات التفصيلي. يُفضَّل إبقاء القائمة فارغة
    | في الإنتاج والاعتماد على جدول الصلاحيات وحده، لكنها تبقى قابلة للضبط
    | حتى لا ينقفل المشرف خارج النظام عند إضافة صلاحية جديدة قبل مزامنتها.
    |
    */

    'super_roles' => array_values(array_filter(array_map(
        'trim',
        explode(',', (string) env('PERMISSION_SUPER_ROLES', 'admin'))
    ))),

];
